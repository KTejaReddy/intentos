services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      INTENTOS_DATA_DIR: /data
      INTENTOS_AI_PROVIDER: heuristic
    volumes:
      - intentos-data:/data
    restart: unless-stopped

volumes:
  intentos-data:
